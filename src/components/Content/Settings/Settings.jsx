import React from 'react';
import s from './Settings.module.css' //CSS import

const Settings = () => {
    return (
        <div>
            Settings
        </div>
    );
};

export default Settings;